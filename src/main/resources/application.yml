spring:
  datasource:
    url: jdbc:sqlserver://localhost:1433;encrypt=true
    username: postgres
    password: 061098
    driver-class-name: org.postgresql.Driver
    jpa:
      hibernate:
        ddl-auto: create-drop
      show-sql: true
      properties:
        hibernate:
          format_sql: true
          database: postgresql
          database-platform: org.hibernate.dialect.PostgreSQLDialect
